
const addPayment = (req, res) => {
  res.status(501).json({ message: 'Not Implemented' });
};

const getPayment = (req, res) => {
  res.status(501).json({ message: 'Not Implemented' });
};

module.exports = {
  addPayment,
  getPayment,
};
